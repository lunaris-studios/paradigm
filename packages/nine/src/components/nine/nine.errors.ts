import * as Neptune from "@paradigmjs/paradigm";

const ns = Neptune.DISPLAYNAME_PREFIX;

export const WARN_NO_HEIGHT = ns + ` <Nine> height prop will default to 100px`;
export const ERROR_NO_IMAGE =
	ns + ` <Nine> component failed to render, no image was provided for the 9-slice`;
export const WARN_NO_CORNER = ns + ` <Nine> cornerLength prop will default to 25px`;
export const WARN_NO_WIDTH = ns + ` <Nine> width prop will default to 100px`;
